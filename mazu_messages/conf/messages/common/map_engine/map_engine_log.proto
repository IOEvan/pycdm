syntax = "proto2";

option cc_enable_arenas = true;
package nio.ad.messages;
import "common/perf/sensor_ts_header.proto";


message MapEngineMeta {
  optional int32 map_version = 1;
  optional string release_tag = 2;
  optional bytes reserved = 3;
}

message MapEngineFunctionLog {
  optional string function_name = 1;
  repeated string function_parameters = 2;
  optional double timestamp = 3;
  optional bytes reserved = 4;
}

message MapEngineLog {
  optional MapEngineMeta meta = 1;
  repeated string raw_logs = 2;
  repeated MapEngineFunctionLog function_logs = 3;

  repeated bytes reserved = 4;

  optional uint64 timestamp = 536870905; // ptp timestamp, nanosecond，used for data bundle readin 
  optional SensorTSHeader sensor_original_ts = 536870906;   // sensor original ts
  optional uint64 publish_ptp_ts = 536870907; // publish ptp timestamp, uint in nanosec
  optional string publisher_id = 536870909;  // uuid of the publisher
  optional uint64 counter = 536870910;       // message counter
  optional uint64 publish_ts = 536870911;    // publish utc timestamp; unit : nanosecond
}
