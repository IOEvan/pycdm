syntax = "proto2";

option cc_enable_arenas = true;
package nio.ad.messages;
import "common/perf/sensor_ts_header.proto";

message HardwareModeBroadcast{
  enum HardwareModeType {
      INVALID_TYPE = 0;
      DEFAULT = 1; //OFF:S2,S4,SideCam-DES; ON:S1,S3,SVC-CDC
      FULL = 2; //ON:All,SVC-CDC
  }
  optional HardwareModeType current_hardware_mode = 1;

  enum VehicleXcuMode {
    INVALID_MODE = 0;
    POWER_DEFAULT_ALL = 1; //PNC DFT, all the vehicle vcu/bcu will power on
    POWER_AD_ONLY = 2; //Only adc domain power on
  }
  optional VehicleXcuMode current_vehicle_xcu_mode = 2;

  optional uint64 timestamp = 536870905; // ptp timestamp, nanosecond，used for data bundle readin   
  optional SensorTSHeader sensor_original_ts = 536870906;   // sensor original ts
  optional uint64 publish_ptp_ts = 536870907;   // publish ptp timestamp unit : nanosecond
  optional string publisher_id = 536870909;    // uuid of the publisher
  optional uint64 counter = 536870910;         // message counter
  optional uint64 publish_ts = 536870911;     // publish utc timestamp; unit : nanosecond
}
