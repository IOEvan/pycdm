syntax = "proto2";

option cc_enable_arenas = true;
package nio.ad.messages;
import "common/map_engine/map_geometry.proto";

message DbgMapContent{
  optional uint64 id = 1;
  repeated MapPoint points = 2; // 所有point按照顺序排列，依照顺序进行绘制
}

message DbgMapLayer{
  optional string layer_name = 1;
  repeated DbgMapContent contents = 2;
}

message DbgMapVisualizeOutputPb {
  repeated DbgMapLayer layers = 1;

  optional uint64 timestamp = 2;

  optional string reserved = 200;  //reserved for some tmp hacking？？ limit 1k string

  optional uint64 publish_ptp_ts = 536870907;   // publish ptp timestamp, uint in nanosec
  optional string publisher_id = 536870909;     // uuid of the publisher
  optional uint64 counter = 536870910;         // message counter
  optional uint64 publish_ts = 536870911;      // publish utc timestamp; unit : nanosecond
}